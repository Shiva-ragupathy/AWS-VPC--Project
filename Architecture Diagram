## 🌐 AWS VPC Architecture

This diagram represents a custom VPC built in Amazon Web Services with public and private subnets, an Internet Gateway, and a Linux EC2 instance deployed in the public subnet.

flowchart TB

    Internet([🌍 Internet])

    IGW[[Internet Gateway]]

    subgraph VPC["VPC 10.0.0.0/16"]
        direction TB

        subgraph PublicSubnet["🟢 Public Subnet (10.0.1.0/24)"]
            EC2["🖥️ Linux EC2 Instance\n(Public IP Enabled)"]
        end

        subgraph PrivateSubnet["🔒 Private Subnet (10.0.2.0/24)"]
            PrivateNote["No Internet Access"]
        end
    end

    Internet --> IGW
    IGW -->|0.0.0.0/0| PublicSubnet

    style Internet fill:#4da6ff,color:#fff,stroke:#1f77b4
    style IGW fill:#ffcc00,color:#000,stroke:#e6b800

    style VPC fill:#f2f2f2,stroke:#999,stroke-width:2px

    style PublicSubnet fill:#d4edda,stroke:#28a745
    style EC2 fill:#cce5ff,stroke:#007bff

    style PrivateSubnet fill:#f8d7da,stroke:#dc3545
    style PrivateNote fill:#fff,stroke:#dc3545,color:#dc3545
